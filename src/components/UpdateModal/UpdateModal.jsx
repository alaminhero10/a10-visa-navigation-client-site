// import { useLoaderData } from "react-router-dom";
// import MyVisaUpdateModal from "../MyVisaUpdateModal/MyVisaUpdateModal";

const UpdateModal = () => {
    // const visaUpdateData = useLoaderData();


    return (
        <div>
            {/* {
                visaUpdateData.map((visaData) => <MyVisaUpdateModal key={visaData._id} visaData={visaData}></MyVisaUpdateModal>)
            } */}
        </div>
    );
};

export default UpdateModal;